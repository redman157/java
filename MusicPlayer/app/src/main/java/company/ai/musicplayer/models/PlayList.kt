package company.ai.musicplayer.models

import androidx.room.*


/*
@Entity(
    tableName = TABLE_NAME,
    foreignKeys = [ForeignKey(
        entity = Director::class,
        parentColumns = ["did"],
        childColumns = [Music.TITLE],
        onDelete = ForeignKey.CASCADE
    )],
    indices = [Index(Music.TITLE)]
)
data class PlayList(
    @PrimaryKey(autoGenerate = true)
    @ColumnInfo(name = NAME) var playListName: String
) {
    companion object{
        const val TABLE_NAME = "play_list"
        const val NAME = "name_play_list"
    }
}*/
